; rtp.conf — RTP port range. Must match the published docker ports
; (10000-10009/udp) so phone audio reaches the container. Kept deliberately
; small: Docker Desktop's forwarder is unreliable with 100+ port UDP ranges,
; and 10 ports still carry 5 concurrent calls.

[general]
rtpstart = 10000
rtpend = 10009
